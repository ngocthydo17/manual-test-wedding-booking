@model IEnumerable<(string Id, doan.Models.AllDish Dish)>
<h1>Món ăn</h1>
    <!-- Form tìm kiếm -->
<form asp-action="Search" method="get">
    <div class="input-group mb-3">
        <input type="text" class="form-control" placeholder="Tìm kiếm món ăn..." name="searchTerm">
        <div class="input-group-append">
            <button class="btn btn-outline-secondary" type="submit">Search</button>
        </div>
    </div>
</form>
<button type="button" class="btn-custom" onclick="location.href='@Url.Action("Create")'">Thêm món ăn</button>
<table class="table">
    <thead>
        <tr>
            <th style="width: 100px;">Ảnh</th>
            <th style="width: 120px;">Tên món ăn</th>
            <th style="width: 100px;">Loại</th>
            <th style="width: 100px;">Giá</th>
            <th style="width: 550px;">Mô tả</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img src="@item.Dish.image" width="100" height="100" />
                </td>
                <td>@item.Dish.name</td>
                <td>@item.Dish.cate</td>
                <td>@item.Dish.price</td>
                <td>@item.Dish.mota</td>
                <td style="align-content: center; padding-left: 50px">
                    <a asp-action="Edit" asp-route-id="@item.Id" style="text-decoration: none;">
                        <i class="fas fa-edit custom-icon-color"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.Id" style="text-decoration: none;">
                        <i class="fa-solid fa-trash custom-icon-color"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>
